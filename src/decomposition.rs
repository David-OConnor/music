//! Infer a composition from PCM audio

use std::{io, path::Path};

use crate::composition::Composition;

pub fn infer_composition(audio_path: &Path) -> io::Result<Composition> {
    unimplemented!()
}
