[package]
name = "music"
version = "0.1.0"
edition = "2024"

[dependencies]
rodio = "0.19"

#burn = { version = "0.20.1", features = ["std", "train", "fusion", "ndarray", "autotune"], default-features = false }
#burn-cpu = "0.20.1"